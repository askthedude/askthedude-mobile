import { View, Text } from "react-native";
import React from "react";

export const GenericModal = ({ children }: { children: any }) => {
  return (
    <View>
      <Text>GenericModal</Text>
    </View>
  );
};
