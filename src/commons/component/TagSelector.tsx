import { View, Text } from "react-native";
import React from "react";

type Props = {};

export const TagSelector = (props: Props) => {
  return (
    <View>
      <Text>TagSelector</Text>
    </View>
  );
};
